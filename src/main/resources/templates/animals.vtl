#if($safeAnimals)
  <h1>Added $safeAnimals.getName()</h1>
#else
  <h1>Add Safe Animal</h1>
#end
#if($safeAnimals)
  #foreach($safeAnimals in $animals)
    <h3>$animals.getName()</h3>
    <h3>$endangeredAnimals.getHealth()</h3>
    <h3>$endangeredAnimals.getAge()</h3>
  #end
#end
#if($endangeredAnimals)
  <h1>Added $safeAnimals.getName()</h1>
#else
  <h1>Add Animal</h1>
#end
#if($safeAnimals)
  #foreach($safeAnimals in $animals)
    <h3>$animals.getName()</h3>
    <h3>$endangeredAnimals.getHealth()</h3>
    <h3>$endangeredAnimals.getAge()</h3>
  #end
#end
<div class="row">
  <div class="col-sm-4"></div>
  <div class="col-sm-4">
  <form action='/animals' method="post">
    <div class="form-group">
      <label for="safeAnimals"> Add a Safe Animal: </label>
      <input id="name" name="name" class="form-control" type="text">
    </div>
    <div class="form-group">
      <label for="endangeredAnimals"> Endangered Animal Name: </label>
      <input id="name" name="name" class="form-control" type="text">
    </div>
    <div class="form-group">
      <label for="endangeredAnimals">Endangered Animal Health: </label>
      <input id="health" name="health" class="form-control" type="text">
    </div>
    <div class="form-group">
      <label for="endangeredAnimals">Endangered Animal Age: </label>
      <input id="age" name="age" class="form-control" type="text">
    </div>
    <button type='submit' class='btn'>Submit Animal</button>
  </form>
</div>
